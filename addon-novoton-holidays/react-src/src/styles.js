/**
 * Novoton Booking Engine - CSS styles
 *
 * Styles are now loaded as a static CSS file (booking-engine.css) via the
 * CS-Cart {style} tag in styles.post.tpl.  No runtime injection needed.
 *
 * This module is kept as a no-op so existing imports don't break.
 */

export function injectStyles() {
    // No-op: styles are loaded via booking-engine.css in the theme CSS directory.
    // Edit: design/themes/nova_theme/css/addons/novoton_holidays/booking-engine.css
}
