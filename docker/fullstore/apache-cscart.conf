# CS-Cart ships .htaccess files (SEO URLs, protection rules); they only take
# effect when the docroot allows overrides.
<Directory /var/www/html>
    AllowOverride All
    Require all granted
</Directory>
