# All outbound mail is captured by the Mailpit container — nothing leaves the
# sandbox. View messages at http://localhost:8025.
defaults
auth off
tls off
logfile /tmp/msmtp.log

account mailpit
host mailpit
port 1025
from store@novotonbun.local

account default : mailpit
